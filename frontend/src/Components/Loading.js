import './Loading.css';

function Loading() {
    return (
        <div>
            <h1>
                Loading
                <span className="dots">
          <span>.</span>
          <span>.</span>
          <span>.</span>
        </span>
            </h1>
        </div>
    );
}

export default Loading;
